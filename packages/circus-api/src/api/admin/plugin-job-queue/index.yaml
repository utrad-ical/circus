name: PluginJobQueue Administration
description: >
  Returns job queue.
routes:
  - verb: get
    path: /admin/plugin-job-queue
    description: >
      Returns plugin job queue.
    requiredGlobalPrivilege: manageServer
    responseSchema: pluginJobQueue|searchResult
