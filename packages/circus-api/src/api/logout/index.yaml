name: Logout
description: >
  Forces the user to log-out by invalidating the access token.
routes:
  - verb: get
    path: /logout
    description: >
      Forces the user to log-out by invalidating the access token.
