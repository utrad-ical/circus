name: Plugin Jobs
description: >
  Dynamically provides CIRCUS CS displays.
routes:
  - verb: get
    path: /plugin-displays/:pluginId/:path+
    requiredPluginPrivilege: readPlugin
    noAuthentication: true
    description: >
      Returns the display using Webpack Module Federation.
