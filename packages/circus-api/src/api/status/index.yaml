name: status
description: Shows server status
routes:
  - verb: get
    path: /status
    description: Shows server status.
  - verb: post
    path: /echo
    handler: handleEcho
    description: Echoes what's in the input JSON.
