doctype html
html
	head
		meta(charset="utf-8")
		meta(http-equiv="X-UA-Compataible", content="IE=edge")
		meta(name="viewport", content="width=device-width,initial-scale=1")
		link(rel='stylesheet', href='css/style.css')
		script(src="dist/circus-rs-client.js")
		script(src="https://code.jquery.com/jquery-2.2.0.min.js")
		script(src="./demo1.js")
		title CIRCUS RS Demo
	body.demo1
		h1 CIRCUS RS DEMO (demo1.js)

		p
			| This sample demonstrates dynamic loading of the series.<br>
			| While the image is loading, Mock image (checkerboard) is displayed for the time being.<br>
			| The two viewers share the same ViewState object.

		canvas#rs-canvas(width=600, height=600)
		.right-column
			canvas#view-state-canvas(width=300, height=300)
			#view-state-canvas-note
				p Use mouse wheel to navigate the image orientation. (Press Ctrl key to speed up)
				div.rect#color-x Move along/rotate around X-axis
				div.rect#color-y Move along/rotate around Y-axis
				div.rect#color-z Move along/rotate around Z-axis
				div.rect#color-scale Zoom
		#config
			p
				label API Endpoint
				input#server(type='text')
			p
				label Target Series UID
				input#series(type='text')
			p: button#start Load
